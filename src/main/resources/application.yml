spring:
  datasource:
    url: jdbc:mysql://localhost:3306/api-batch?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: ibiz1234!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        use_sql_comments: true
        highlight_sql: true
        type:
          binder:
            log: true
  main:
    debug: true

logging:
  level:
    org:
      hibernate:
        SQL: debug
        type: trace
    com.example.drugtrack.controller: INFO


